node_modules
.next
.env.local
.env
.DS_Store
*.bak
# Shared agent config (.claude/settings.json, .claude/hooks/) is committed on
# purpose so every session gets it. Only personal overrides stay local.
.claude/settings.local.json
.vercel
eslint.json
